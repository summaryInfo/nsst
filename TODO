Until version 1.6:
    OSC 50 ; Pt ST -- Set font
    Refactor config
        Split global and perinstance settings, reload reliably
        Allow geometry to be specified in characters
        Implement named values for options instead of numeric
    Refactor debug output
        make it more granular
            --trace-characters
            --trace-escapes
            --trace-fonts
            --trace-events
            --trace-misc
===================
General
    Write man page
    Allow short lines to be seleted properly
    Some alternative to XParseColor
    Implement proper daemon mode
    Make X resource/option names more or less correspond to Xterm/rxvt-unicode?
Render
    General
        Colored glyphs
        Double width/height
        Down-Line-Loadable fonts
        Add options to select width of ambigues characters
        Combining characters support
        Add fallback fonts support back (optionally enabled)
        Refactor frame timings
            use separate timings for erase,scroll,resize
            just wait for screen update completion?
                do not redraw if erase/scroll/resize reached
                    or probably just wait until nothing read
                    from master in main loop
                draw if max frame time reached
        Window operations
            Make it work with reparenting
    XRender
        Slow scrolling of full-color images (with small font in chafa)
        Use multiple separate buffers for background/lines/glyphs to sort less
    Wayland
Emulation
    Proper vt level restriction
    SL,SR: If the current cursor position is outside of the current scroll region it does nothing???
    Printing with SGR
    More advanced printing method (command with pipe)
    Is private mode 1044 semantics correct/sould it be implemented at all?
    Is reset correctly implemented?
    VT220
        CSI ? 4 l / CSI ? 4 h -- DECSCLM
        CSI ? 8 l / CSI ? 8 h -- DECARM (When ported to wayland)
        DCS Pf ; Pc ; Pe ; Pw ; Ps ; Pt; Ph ; Px f Pt ST -- DECDLD
        DECDHL/DECDHL/DECSWL/DECDWL
    SIXEL
        CSI ? 80 l / CSI ? 80 h -- DECSDM
        CSI ? 1070 l / CSI ? 1070 h -- Use private resgisters
        CSI ? 8452 l / CSI ? 8452 h -- Sixel scrolling leaves cursor to the right of graphics
        CSI ? Pi ; Pa; Pv S -- XTSMGRAPHICS (Pi = 1, 2; Pa = 1, 2, 3, 4)
        DSC Pa ; Pb ; Ph q Ps..Ps ST -- Send sixel image
    Misc (won't be implemented any time soon)
        CSI ? 38 l / CSI ? 38 h -- DECTEK
        CSI ? 1001 l / CSI ? 1001 h -- Hightlight mouse tracking
        CSI ? 1040 l / CSI ? 1040 h -- terminfo/termcap function key mode [input]
        CSI ? 1039 l / CSI ? 1039 h -- Alt sends escape [input]
        CSI ? 2001 l / CSI ? 2001 h -- srm_BUTTON1_MOVE_POINT
        CSI ? 2002 l / CSI ? 2002 h -- srm_BUTTON2_MOVE_POINT
        CSI ? 2003 l / CSI ? 2003 h -- srm_DBUTTON3_DELETE
        CSI ? 14 l / CSI ? 14 h -- Cursor blinking XORing
        CSI ? 35 l / CSI ? 35 h -- font shifting functions
        CSI Ps ; Ps ; Ps ; Ps ; Ps T -- XTHIMOUSE
        CSI Pm # p, CSI Pm # { -- XTPUSHSGR
        CSI # q, CSI # } -- XTPOPSGR
        CSI Pm # Q -- XTPOPCOLORS
        CSI Pm # P -- XTPUSHCOLORS
        CSI # R -- XTREPORTCOLORS
        CSI > Ps p -- XTSMPOINTER
        CSI > Ps q -- XTVERSION -> DCS > | text ST
        CSI > Ps p -- Pointer mode
        DCS + p Pt ST -- XTSETTCAP
        DCS + q Pt ST -- XTGETTCAP
        DCS + Q Pt ST -- XTGETXRES -> DCS Ps + R Pt ST
        OSC I Pt ST -- Set icon XPM file
        OSC 3 ; Pt ST -- Set X property
        OSC 13 Pt ST -- Set mouse background color
        OSC 14 Pt ST -- Set mouse foreground color
        OSC 113 Pt ST -- Reset color mouse foreground color
        OSC 114 Pt ST -- Reset color mouse background color
